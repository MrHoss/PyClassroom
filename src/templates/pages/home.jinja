{% extends 'components/navmenus/lateralmenu.jinja' %}
{% block content %}
<div class="TitleHeader">
    <input class="searchFields" placeholder="Pesquisar..." type="search"/>
</div>
<div class="Paper">
    <h2 class="SubTitle">Olá aluno seja bem vindo!</h2>
    <div class="columnContainer">
    <div>
    {% for i in interface %}
        <div class="listItem">
            <div style="width:50%"><h6 class="listTitle">{{i['discipline']}}</h6><div><button class="navBtn listBtn" value="studying/{{i['nameID']}}">Acessar</button></div></div>
            <div style="display:flex;justify-content:right;width:50%;align-items:center;"><progress style="right:0;"value='' max="100"></progress></div>
        </div>
    {% endfor %}
    </div>
    <div>
        <button value='quizzpp' class="navBtn">Jogos</button>
    </div>
    <div>
        <h3>Novidades</h3>
    </div>
    </div> 
</div>

{% endblock %}